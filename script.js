const navToggle=document.querySelector('.nav-toggle');const siteNav=document.querySelector('#site-nav');if(navToggle&&siteNav){navToggle.addEventListener('click',()=>{const open=siteNav.classList.toggle('is-open');navToggle.setAttribute('aria-expanded',String(open));});siteNav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{siteNav.classList.remove('is-open');navToggle.setAttribute('aria-expanded','false');}));}
const fitChecker=document.querySelector('#fitChecker');const fitResult=document.querySelector('#fitResult');if(fitChecker&&fitResult){fitChecker.addEventListener('submit',event=>{event.preventDefault();const data=new FormData(fitChecker);let score=0;if(['bachelor','masters','professional'].includes(data.get('degree')))score+=2;if(['ai','business','hospitality','collaboration'].includes(data.get('interest')))score+=2;if(data.get('readiness')==='strong')score+=3;if(data.get('readiness')==='medium')score+=2;if(data.get('english')==='ready')score+=3;if(data.get('english')==='preparing')score+=1;let title='Early preparation profile';let body='You may need document preparation, English readiness, and program-fit guidance before moving forward.';if(score>=8){title='Strong initial fit';body='Your profile appears ready for a serious graduate pathway conversation. Prepare academic records, English evidence, CV, and a clear statement of purpose.';}else if(score>=5){title='Promising but needs review';body='Your profile has potential, but academic documents, English readiness, and program alignment should be reviewed before application planning.';}fitResult.classList.add('is-visible');fitResult.innerHTML=`<h3>${title}</h3><p>${body}</p><a class="button" href="#contact">Request guidance</a>`;});}
const contactForm=document.querySelector('#contactForm');if(contactForm){contactForm.addEventListener('submit',event=>{event.preventDefault();const data=new FormData(contactForm);const subject=encodeURIComponent(`Portfolio inquiry: ${data.get('type')}`);const body=encodeURIComponent(`Name: ${data.get('name')}\nEmail: ${data.get('email')}\nInquiry type: ${data.get('type')}\n\nMessage:\n${data.get('message')}`);window.location.href=`mailto:na@kduniv.ac.kr?subject=${subject}&body=${body}`;});}